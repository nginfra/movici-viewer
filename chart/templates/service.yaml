apiVersion: v1
kind: Service
metadata:
  name: {{ include "webconsole.fullname" . }}
  labels:
    {{- include "webconsole.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
  selector:
    {{- include "webconsole.selectorLabels" . | nindent 4 }}
